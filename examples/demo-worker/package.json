{
  "name": "demo-worker",
  "version": "0.1.0",
  "private": true,
  "description": "A Cloudflare Worker on D1: the smallest project sandboxr can run end to end.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "migrate": "wrangler d1 migrations apply demo --local"
  },
  "devDependencies": {
    "wrangler": "4.125.0"
  }
}
